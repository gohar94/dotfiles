# dotfiles

## Getting the important tools/configs set up
`./init.sh`

## Getting all the tools/configs set up
`./init.sh --all`

## Only changing the shell to fish
`./init.sh --chsh`

## Sync all configs to latest changes and pull any new tools
`./sync.sh`
