set -g default-terminal "screen-256color"

# Mouse: select/copy within a single pane; OSC 52 sends copies to local clipboard over SSH
set -g mouse on
set -s set-clipboard on
set -as terminal-features ',*:clipboard'
